(library
 ((name ppx_enumerate)
  (public_name ppx_enumerate)
  (public_release ((kind ppx_type_conv_plugin)))
  (wrapped false)
  (libraries (ocamlcommon ppx_core ppx_type_conv))
  (preprocess ((metaquot All)))))
